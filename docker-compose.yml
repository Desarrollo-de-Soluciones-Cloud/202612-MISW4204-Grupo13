services:
  seneprojects_frontend:
    build:
      context: ./frontend
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-/api}
    container_name: seneprojects_frontend
    ports:
      - "80:8080"
    restart: always
